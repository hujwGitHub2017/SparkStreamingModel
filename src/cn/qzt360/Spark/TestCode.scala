package cn.qzt360.Spark

object TestCode {
  def main(args: Array[String]): Unit = {
      val code:String = "FA169605000240"
      println("id = "+code.substring(code.length()-2,code.length()))
  }
}